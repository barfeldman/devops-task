HashiCorp Vault secret management - proof (Thu Aug 27 00:57:50 IDT 2026)

== 1. Source of truth: Vault KV v2 (value never leaves Vault, never in Git) ==
created_time            2026-08-26T20:47:16.545748333Z
current_version         1
max_versions            0
created_time     2026-08-26T20:47:16.545748333Z

== 2. Least-privilege policy + Kubernetes auth role bound to the app ServiceAccount ==
path "secret/data/sample-nodejs" {
  capabilities = ["read"]
}
audience                                    vault
bound_service_account_names                 [sample-nodejs]
bound_service_account_namespace_selector    n/a
bound_service_account_namespaces            [sample-nodejs]
token_policies                              [sample-nodejs]

== 3. Vault Secrets Operator synced the secret (all READY) ==
NAME                                                  HEALTHY   READY   AGE
vaultconnection.secrets.hashicorp.com/sample-nodejs   True      True    38m

NAME                                            HEALTHY   READY   AGE
vaultauth.secrets.hashicorp.com/sample-nodejs   True      True    38m

NAME                                                    SYNCED   HEALTHY   READY   AGE
vaultstaticsecret.secrets.hashicorp.com/sample-nodejs   True     True      True    38m

== 4. Native K8s Secret produced by VSO (keys only, values hidden) ==
keys: ['API_TOKEN', '_raw']

== 5. App consumes it via envFrom; logs only that it is set ==
API_TOKEN configured: true

== 6. End-to-end through the nginx ingress ==
  GET /classified  (no token)     -> HTTP 401
  GET /classified  (wrong token)  -> HTTP 401
  GET /classified  (Vault token)  -> HTTP 200  Access granted to classified data.

== 7. Secret value in NO tracked Git file; .vault/ (unseal keys + root token) is gitignored ==
